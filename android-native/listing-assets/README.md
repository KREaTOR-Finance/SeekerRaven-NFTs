# Listing Assets

The files in this folder are placeholders reused from the current collection artwork so the Solana Mobile publishing config has concrete paths.

Before submission, replace them with store-ready assets that satisfy the current Solana Mobile dApp Store media requirements.

Current source files referenced by `android-native/dapp-store/config.yaml` live in `android-native/dapp-store/media/`.
